# Skip optional dependencies
optional=false

# Force legacy peer dependencies to avoid compatibility issues
legacy-peer-deps=true

# Disable engine strict to avoid version conflicts
engine-strict=false

# Prefer offline mode to use cached packages when possible
prefer-offline=true 