generator client {
  output   = "./src/generated.rs"
  provider = "mise prisma"
}

datasource db {
  provider = "postgresql"
  // FIXME
  url      = env("DIRECT_URL")
}

model User {
  id       Int    @id @default(autoincrement())
  username String @unique
  email    String @unique
}
